services:
  routussy:
    build:
      context: .
      network: host
    network_mode: host
    env_file:
      - .env
    environment:
      DATABASE_PATH: /data/routussy.db
      MEDIA_ROOT: /data/media
    volumes:
      - routussy-data:/data

volumes:
  routussy-data:
