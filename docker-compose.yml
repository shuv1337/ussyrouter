services:
  routussy:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DATABASE_PATH: /data/routussy.db
    volumes:
      - routussy-data:/data

volumes:
  routussy-data:
