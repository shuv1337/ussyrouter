services:
  routussy:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DATABASE_PATH: /data/routussy.db
      MEDIA_ROOT: /data/media
    volumes:
      - routussy-data:/data

volumes:
  routussy-data:
